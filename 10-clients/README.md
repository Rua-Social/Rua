# Client material

Live engagement records do not live in this repository.

Named client facts, fees, scopes, and project paths are retrieved on
purpose through `rua vault search` then `rua vault get`. Do not add
client documents, names, or location maps here.

If the vault is missing or a record fails verification, say so. Do not
search Desktop, Downloads, Git history, or mounted disks as a fallback.
