# Git history

The Git checkout is the current tree only.

Older Git history is an external vault artefact. Retrieve it on
purpose with `rua vault` if a past commit is actually required.
Do not clone, grep, or restore that archive as default context.
Do not put it back into this repository.
