# 00-system

The cross-cutting operating layer: things used across clients and projects
rather than belonging to any one of them.

- `skills/` — reusable procedures Claude runs repeatedly (see each skill's
  own `SKILL.md`)
- `templates/` — starting-point documents/structures reused across projects
