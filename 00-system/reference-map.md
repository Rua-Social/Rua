# Library

An index of real artefacts. Most of them live outside Git.

Consult on purpose through `rua vault search` then `rua vault get`.
Never default context for a production chat.

Named entries, paths, and commercial terms are not stored here.
