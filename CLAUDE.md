# Operating instructions for Claude in this repo

@AGENTS.md

This adapter imports the shared operating rules. Keep shared policy in
`AGENTS.md` and its referenced doctrine so every harness uses the same contract.
For client load order and precedent, follow `00-system/rewrite-contract.md`.
Load only the material relevant to the requested job.
